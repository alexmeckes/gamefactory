{
  "name": "gamefactory.agent-team",
  "version": "0.1.0",
  "apiVersion": "1.0",
  "entry": "dist/index.js",
  "activation": ["agent:agent.team"],
  "contributes": { "agent": ["agent.team"] },
  "permissions": ["process:configured-command", "filesystem:candidate", "git:status"],
  "description": "A bounded legacy team pipeline or declarative, authority-scoped agent graph with versioned writer generations, claim-linked review findings, structured handoffs, repair edges, and serialized writers."
}
