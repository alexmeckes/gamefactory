{
  "name": "gamefactory.mock",
  "version": "0.1.0",
  "apiVersion": "1.0",
  "entry": "dist/index.js",
  "activation": ["workspace:mock.workspace", "agent:mock.agent", "evaluator:mock.score"],
  "contributes": {
    "workspace": ["mock.workspace"],
    "agent": ["mock.agent"],
    "evaluator": ["mock.score"]
  },
  "description": "Deterministic lightweight drivers for smoke tests and extension development."
}
