{
  "name": "gamefactory.command-agent",
  "version": "0.1.0",
  "apiVersion": "1.0",
  "entry": "dist/index.js",
  "activation": ["agent:command.agent"],
  "contributes": { "agent": ["command.agent"] },
  "permissions": ["process:configured-command", "filesystem:candidate"],
  "description": "Model-agnostic adapter that runs a configured coding-agent command inside an isolated candidate."
}
